import * as angular from 'angular'
import * as React from 'react'
import {react2angular} from 'react2angular'
import * as PropTypes from 'prop-types'

import './{{dashCase name}}.scss'
// import './{{dashCase name}}.html'
// import other stuff here

// these are the typescript proptypes, effectively
export interface {{pascalCase name}}Interface {
  {{{typescript types}}}
}
// these props are used by react2angular and various other downstream consumers
export const propTypes = {
  // write your propTypes here, a la https://reactjs.org/docs/typechecking-with-proptypes.html
  {{{proptypes types}}}
}

// this is our react component
export const {{pascalCase name}}: React.StatelessComponent<{{pascalCase name}}Interface> = (props: {{pascalCase name}}Interface) => {
  return ({{{html2jsx (pascalCase name) template}}})
}
// needs propTypes for react2angular to work
{{pascalCase name}}.propTypes = propTypes

// let's export the angular nonsense
export const DistributeAngular{{pascalCase name}} = angular.module(`distribute.components.{{dashCase name}}`, [])
  .component(`{{camelCase name}}`, react2angular({{pascalCase name}}))
